/*
 * BundleInfo Messages
 *
 * This contains all the text for the BundleInfo component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BundleInfo';

export default defineMessages({
  resources: {
    id: `${scope}.resources`,
    defaultMessage: 'Resources: ',
  },
});
