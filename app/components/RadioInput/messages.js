/*
 * RadioInput Messages
 *
 * This contains all the text for the RadioInput component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RadioInput';

export default defineMessages({
  defaultRadioItem: {
    id: `${scope}.defaultRadioItem`,
    defaultMessage: 'Default Radio Item',
  },
  defaultErrorMessage: {
    id: `${scope}.defaultErrorMessage`,
    defaultMessage: 'Default Error Message',
  },
  defaultRadioLabel: {
    id: `${scope}.defaultRadioLabel`,
    defaultMessage: 'Default Radio Label',
  },
});
