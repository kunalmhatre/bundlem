/*
 * BundleCard Messages
 *
 * This contains all the text for the BundleCard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BundleCard';

export default defineMessages({
  res: {
    id: `${scope}.res`,
    defaultMessage: ' Res.',
  },
  view: {
    id: `${scope}.view`,
    defaultMessage: 'view',
  },
});
